<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MarvelApp30" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/black1</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/gray2</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?android:attr/colorSecondary</item>

        <item name="colorSurface">@color/white</item>

        <item name="android:colorBackground">@color/white</item>
        <item name="colorOnBackground">@color/black1</item>
    </style>

    <style name="Theme.MarvelApp30" parent="Base.Theme.MarvelApp30" />

    <style name="ShapeAppearanceOverlay.App.ImageShape" parent="">
        <item name="cornerSizeTopLeft">4dp</item>
        <item name="cornerFamilyTopLeft">rounded</item>
        <item name="cornerSizeBottomLeft">4dp</item>
        <item name="cornerFamilyBottomLeft">rounded</item>
    </style>

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/red</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.MarvelApp30</item>
    </style>
</resources>